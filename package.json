{
  "name": "assignment-nz-fullstack-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "api": "turbo run api",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "db:seed": "cd apps/api && pnpm db:seed",
    "db:reset": "cd apps/api && pnpm db:reset"
  },
  "devDependencies": {
    "turbo": "^1.10.12"
  },
  "packageManager": "pnpm@8.0.0"
}